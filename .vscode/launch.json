{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Current File",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",  // Ensure this path points to your main.py file
            "console": "integratedTerminal"
        }
    ]
}
